export const TextTags =
  'p' ||
  'h1' ||
  'h2' ||
  'h3' ||
  'h4' ||
  'h5' ||
  'h6' ||
  'b' ||
  'i' ||
  'u' ||
  'li' ||
  'code' ||
  'nav' ||
  'mark' ||
  'strong' ||
  'small' ||
  'sub' ||
  'abbr' ||
  'address' ||
  'blockquote'

export const TouchableTags = 'button' || 'a'

export const ContentTags =
  'div' || 'span' || 'section' || 'article' || 'header' || 'footer' || 'main' || 'thead' || 'tbody' || 'tfoot' || 'hr'

export const IgnoredTags = 'head' || 'canvas' || 'caption' || 'iframe' || 'svg' || 'var' || 'video' || 'audio' || 'cite'
